@model ToppingCreateEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>@Resources.Views.Shared.EditShared.Edit</h1>

<h4>Topping</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Topping.Name" class="control-label"></label>
                <input asp-for="Topping.Name" class="form-control" />
                <span asp-validation-for="Topping.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Topping.Price" class="control-label"></label>
                <input asp-for="Topping.Price" class="form-control" />
                <span asp-validation-for="Topping.Price" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Topping.Id" />
            <input type="hidden" asp-for="Topping.CreatedAt" />
            <input type="hidden" asp-for="Topping.CreatedBy" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
