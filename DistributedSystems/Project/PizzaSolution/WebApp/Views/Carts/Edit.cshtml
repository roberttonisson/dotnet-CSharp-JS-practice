@model CartCreateEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>@Resources.Views.Shared.EditShared.Edit</h1>

<h4>Cart</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Cart.UserId" class="control-label"></label>
                <select asp-for="Cart.UserId" class="form-control" asp-items="Model.AppUserSelectList"></select>
            </div>
            <input type="hidden" asp-for="Cart.Id" />
            <input type="hidden" asp-for="Cart.CreatedAt" />
            <input type="hidden" asp-for="Cart.CreatedBy" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
