@model BLL.App.DTO.AdditionalTopping

@{
    ViewData["Title"] = "Create";
}

<h1>@Resources.Views.Shared.CreateShared.Create</h1>

<h4>AdditionalTopping</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ToppingId" class="control-label"></label>
                <select asp-for="ToppingId" class ="form-control" asp-items="Model.ToppingSelectList"></select>
            </div>
            <div class="form-group">
                <label asp-for="PizzaInCartId" class="control-label"></label>
                <select asp-for="PizzaInCartId" class ="form-control" asp-items="Model.PizzaInCartSelectList"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" cli />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
